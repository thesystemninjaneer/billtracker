DB_HOST=db # This refers to the service name in docker-compose.yml
DB_USER=bill_user
DB_PASSWORD=your_db_password # ⚠️ Use the same password as in docker-compose.yml
DB_NAME=bill_tracker_db
DB_PORT=3306
SERVICE_PORT=3000 # Port for this service (different from org service)
JWT_SECRET=your_jwt_secret_key_very_long_and_random # ⚠️ Generate a strong, random key!
FRONTEND_URL=http://localhost:5173 # URL of the frontend application
ALLOWED_ORIGIN=http://localhost:8080
