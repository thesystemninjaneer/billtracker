const config = {
  ORGANIZATION_API_BASE_URL: 'http://localhost:3001/organizations',
  USER_API_BASE_URL: 'http://localhost:3000', // Base URL for User Service
  // We'll add other service URLs here later, e.g., BILL_API_BASE_URL
};

export default config;