const config = {
  ORGANIZATION_API_BASE_URL: 'http://localhost:3001/organizations',
  // We'll add other service URLs here later, e.g., USER_API_BASE_URL, BILL_API_BASE_URL
};

export default config;